{
  "name": "garten",
  "version": "1.1.0",
  "description": "Animated canvas garden that grows over time. Zero dependencies.",
  "author": "Ade Oshineye",
  "license": "MIT",
  "keywords": [
    "animation",
    "canvas",
    "garden",
    "flowers",
    "plants",
    "background",
    "decorative",
    "generative",
    "procedural",
    "typescript"
  ],
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      },
      "require": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run verify",
    "check:dist": "es-check es2018 dist/index.js dist/index.global.js && es-check es2018 dist/index.mjs --module",
    "verify": "npm run typecheck && npm run test:run && npm run build && npm run check:dist",
    "test:e2e": "npm run build && playwright test",
    "test:visual": "playwright test --project=visual",
    "test:contract": "playwright test --project=contract",
    "test:mutation": "stryker run",
    "test:mutation:core": "stryker run --mutate \"src/defaults.ts,src/palettes.ts,src/GrowthProgress.ts,src/plants/generator.ts\""
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@vitest/coverage-v8": "^2.0.0",
    "es-check": "^9.6.4",
    "fast-check": "^4.5.3",
    "jsdom": "^24.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=16"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adewale/garten.git"
  },
  "homepage": "https://github.com/adewale/garten#readme",
  "bugs": {
    "url": "https://github.com/adewale/garten/issues"
  }
}
